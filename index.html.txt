<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A Special Wish for You</title>
    <style>
        :root {
            --pink: #ffafcc;
            --light-pink: #ffc8dd;
            --blue: #a2d2ff;
            --white: #ffffff;
        }

        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: linear-gradient(135deg, var(--light-pink), var(--blue));
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow: hidden;
            color: #555;
        }

        .card {
            background: rgba(255, 255, 255, 0.85);
            padding: 2rem;
            border-radius: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            max-width: 400px;
            z-index: 10;
            border: 4px solid var(--white);
            animation: float 3s ease-in-out infinite;
        }

        h1 {
            color: #ff4d6d;
            font-size: 2.5rem;
            margin-bottom: 10px;
        }

        p {
            font-size: 1.1rem;
            line-height: 1.6;
        }

        .year {
            font-weight: bold;
            color: #7209b7;
            font-size: 3rem;
            display: block;
            margin: 10px 0;
        }

        .heart-btn {
            background-color: #ff4d6d;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 50px;
            font-size: 1rem;
            cursor: pointer;
            transition: transform 0.3s;
            margin-top: 20px;
            box-shadow: 0 5px 15px rgba(255, 77, 109, 0.4);
        }

        .heart-btn:hover {
            transform: scale(1.1);
        }

        /* Floating Hearts Animation */
        .heart {
            position: absolute;
            color: #ff4d6d;
            font-size: 20px;
            user-select: none;
            pointer-events: none;
            animation: fly 5s linear infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        @keyframes fly {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-10vh) rotate(360deg); opacity: 0; }
        }
    </style>
</head>
<body>

    <div class="card">
        <h1>Hi Sunshine! ✨</h1>
        <p>I wanted to be the first one to tell you...</p>
        <span class="year">2026</span>
        <p>...is going to be amazing, but it would be even better if I get to spend more time with you.</p>
        <p><strong>Happy New Year!</strong></p>
        <button class="heart-btn" onclick="addHearts()">Click for a Surprise</button>
    </div>

    <script>
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = (Math.random() * 3 + 2) + 's';
            document.body.appendChild(heart);

            setTimeout(() => {
                heart.remove();
            }, 5000);
        }

        // Create initial slow hearts
        setInterval(createHeart, 500);

        // Burst of hearts on button click
        function addHearts() {
            for(let i=0; i<20; i++) {
                setTimeout(createHeart, i * 100);
            }
            alert("You make my 2026 feel bright already! 😊");
        }
    </script>
</body>
</html>